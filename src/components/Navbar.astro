---
import LogoMark from "@/components/LogoMark.astro";

interface Props {
  showHome?: boolean;
}

const { showHome = false } = Astro.props;

const NAV_LINKS = [
  ...(showHome ? [{ href: "/", label: "Inicio" }] : []),
  { href: "/servicios", label: "Servicios" },
  { href: "/proyectos", label: "Proyectos" },
  { href: "/contacto", label: "Contacto" },
];
---

<header
  class="fixed top-0 inset-x-0 z-50 border-b border-white/5 bg-ax-void/70 backdrop-blur-md"
>
  <nav
    class="mx-auto max-w-7xl px-5 sm:px-8 h-16 sm:h-20 flex items-center justify-between gap-4"
    aria-label="Navegación principal"
  >
    <!-- Marca: isotipo oficial + wordmark -->
    <a href="/" class="flex items-center gap-3 group" aria-label="Agencia AX - Inicio">
      <LogoMark class="h-14 w-14 sm:h-16 sm:w-16" imageClass="h-[112%] w-[112%]" />
      <span class="font-display tracking-tightest text-lg sm:text-xl font-semibold">
        Agencia <span class="text-ax-gold">AX</span>
      </span>
    </a>

    <!-- Enlaces (desktop) -->
    <ul class="hidden md:flex items-center gap-8">
      {
        NAV_LINKS.map((l) => (
          <li>
            <a
              href={l.href}
              class="text-sm text-white/75 hover:text-white transition-colors"
            >
              {l.label}
            </a>
          </li>
        ))
      }
    </ul>

    <!-- CTA -->
    <a
      href="/contacto"
      class="inline-flex items-center justify-center h-10 sm:h-11 px-4 sm:px-5 rounded-full text-sm font-medium bg-ax-gold text-ax-void hover:bg-ax-goldSoft shadow-goldGlow transition-colors"
    >
      Cotizar
    </a>
  </nav>
</header>
